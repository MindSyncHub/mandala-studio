

## 复刻旧版 UI 样式计划

根据截图对比，需要改造以下页面以匹配旧版设计，保持现有代码架构不变。

### 1. `src/pages/Clients.tsx` — 客户管理

当前：2列卡片，缺少详细联系信息和引用语。

改为：3列网格（`lg:grid-cols-3`），每张卡片包含：
- 彩色圆形头像（用不同 hsl 色调区分客户，如金色、棕色、橄榄色）
- 姓名 + 年龄 + 状态 Badge
- 电话、邮箱各一行（带图标）
- 底部：已完成 N 次疗愈 + 日期 + 引用语（如"焦虑情绪改善中"）
- 页面标题不带图标，副标题"管理和查看所有客户信息"
- "新增客户"按钮改为右上角悬浮样式（带主色背景）

### 2. `src/pages/Cases.tsx` — 个案记录

当前：占位页面，无内容。

改为完整的个案列表页：
- 标题"个案记录" + 副标题"查看和管理所有疗愈个案"
- 右上角"新建记录"按钮（主色）
- 每条记录为一个 Card，包含：头像 + 姓名 + Badge（曼陀罗绘画疗愈/初次评估/深度疗愈）+ 日期
- 描述文字段落
- 两个小 Badge：色彩主题 + 情绪状态
- 底部"疗愈进度"进度条 + 百分比（用渐变色条，不同客户用不同色调）
- 右侧箭头表示可展开/跳转
- Mock 4 条数据（李明65%、张雪20%、刘伟80%、陈静）

### 3. `src/pages/Plans.tsx` — 疗愈方案

当前：占位页面。

改为 3 列卡片布局：
- 标题"疗愈方案" + 副标题"制定和跟踪个性化疗愈方案"
- 右上角"创建方案"按钮
- 每张卡片：方案名 + "执行中" Badge + 客户名·周数 + 进度条百分比
- 下方 checklist（5-6 项），已完成项带绿色勾，未完成带空圆
- 底部"查看详情 →"按钮
- Mock 3 个方案：焦虑缓解疗愈方案(50%)、自我探索成长方案(40%)、压力释放方案(83%)

### 4. `src/pages/AiInterpret.tsx` — AI 解读

当前布局基本正确，微调：
- 标题去掉 Sparkles 图标，改为纯文字"AI 曼陀罗解读"
- 副标题改为"上传曼陀罗绘画作品，获取基于五行色彩体系的AI辅助解读"
- 上传区标题改为"上传曼陀罗作品"，副标题"支持 JPG、PNG 格式，AI将从色彩、构图、五行等多维度进行深度解读"
- 上传按钮文字改为"选择图片上传"
- FeatureCards 改为 4 列（`sm:grid-cols-4`），描述文字与截图对齐

### 5. `src/pages/History.tsx` — 解读历史

当前：列表式卡片 + 主题筛选 Badge。

改为：
- 标题"解读历史" + 副标题"回顾和对比过往的AI曼陀罗解读记录"
- 去掉搜索和主题筛选 Badge
- 卡片改为水平布局：左侧缩略图占位方块（浅棕色背景）+ 右侧内容区
- 内容区：Badge 行（主题 + 状态如"已分享"）+ 日期 + 报告预览文字（截断）
- 底部操作栏：居中"查看详情"按钮 + 分享图标 + 删除图标
- Mock 数据中加入预览文字和状态信息

### 6. `src/components/InterpretUpload.tsx`

- 上传图标改为 Sparkles 类的星形图标（匹配截图中的 ✦ 样式）
- 文字改为"上传曼陀罗作品" + 副标题对齐截图
- 增加"选择图片上传"按钮（主色，outline 样式）

### 7. `src/components/FeatureCards.tsx`

- 改为 4 列布局（`sm:grid-cols-2 lg:grid-cols-4`）
- 图标和描述文字与截图对齐：五行色彩分析 / 三圈结构解读 / 心理映射 / 疗愈建议

### 不改动的文件

- `src/index.css`、`tailwind.config.ts` — 配色体系已正确
- `src/components/AppSidebar.tsx`、`src/components/Layout.tsx` — 侧边栏已匹配截图
- `src/components/DashboardHero.tsx`、`src/pages/Dashboard.tsx` — 首页已匹配
- API 层、路由配置 — 保持不变

